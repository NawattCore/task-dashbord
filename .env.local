# Required for NextAuth (v5 beta) to sign/verify JWTs
AUTH_SECRET=your-long-random-secret

# Local dev URL
NEXTAUTH_URL=http://localhost:3000

# Optional: if you prefer env over code for trustHost
# AUTH_TRUST_HOST=true